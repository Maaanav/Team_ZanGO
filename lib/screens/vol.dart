import 'package:flutter/material.dart';

class Vol extends StatelessWidget {
  const Vol({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
